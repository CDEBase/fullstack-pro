query getPerson($id: String!){
    getPerson(id: $id) {
        id
        name
        sex
        matches {
            id
            name
            sex
            matches {
                id
                name
                sex
                matches {
                    id
                    name
                    sex
                }
            }
        }
    }
}